export type ProjectCardType = {
    contentCard: {
        cards: {
            name: string,
            title: string,
            description: string,
            titleButton: string,
            hrefButton: string,
            srcImage: string,
            altText: string,
        }[];
    };
};